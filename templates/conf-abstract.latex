\begin{conf-abstract}[$if(session)$$date$\\$time$\\Session $session.id$$endif$]
  {$title$}
  {$first_name$ $last_name$}
  {$institution$}
  {$if(email)$$email$$endif$}
  {$if(url)$$url$$endif$}
  {$has_files$}
\indexauthors{$if(index_name)$$index_name$@$endif$$last_name${,} $first_name$}
$body$
$if(biblio)$
\begin{abstractbibliography}
  $for(biblio)$
  \item $biblio$
  $endfor$
\end{abstractbibliography}
$endif$
\end{conf-abstract}

